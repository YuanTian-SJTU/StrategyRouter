{"sample_order": 11, "strategy": "Harmonic", "function": "def priority(item: float, bins: np.ndarray) -> np.ndarray:\n    \"\"\"Returns priority with which we want to add item to each bin with Harmonic strategy.\n\n    Args:\n        item: Size of item to be added to the bin.\n        bins: Array of capacities for each bin.\n\n    Return:\n        Array of same size as bins with priority score of each bin.\n    \"\"\"\n    slack = bins - item\n    non_negative_slack = np.maximum(slack, 0)\n    harmonic_scores = 1 / (1 + non_negative_slack)\n    return harmonic_scores\n\n", "score": -212.0}